# 前端环境变量示例（复制为 .env.local 本地使用，或在 Vercel 控制台配置）
# EXPO_PUBLIC_ 前缀的变量会在构建时被 Expo 内嵌到产物中，可在代码中通过
# process.env.EXPO_PUBLIC_XXX 访问。

# 后端 API 地址（生产环境）
# 本地不配置时默认走硬编码的 https://api.hacktravel.app
EXPO_PUBLIC_API_URL=https://travel.offer.cc.cd
